// Activity 1: Git Checkout
pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                checkout scm  // This pulls the latest code from the Git repository
            }
        }
    }
}
